services:

  bark-server:
    image: ghcr.io/finb/bark-server:latest
    container_name: bark-server
    restart: unless-stopped

    expose:
      - "8080"

    volumes:
      - ./data:/data

    networks:
      - homelab-net

networks:
  homelab-net:
    external: true
