sync:
  - AliCR

platform:
  - linux/amd64
  - linux/arm64

dest:
  AliCR:
    registry: ${ALIYUN_REGISTRY_DOMAIN}
    namespace: ${ALIYUN_REGISTRY_SPACE_NAME}
    flatten: true

sources:
  docker.io:
    images:
      louislam/dockge: 1
      louislam/uptime-kuma: 2
      gdy666/lucky: v2
      library/registry: 3
      library/traefik: v3.7
      joxit/docker-registry-ui: main
      tecnativa/docker-socket-proxy: master
      # 以下省略了 latest
      cloudflare/cloudflared:
      metacubex/mihomo:
      sigoden/dufs:

  ghcr.io:
    images:
      usememos/memos: 0.30.0 # iPhone app 支持的版本
      tailscale/tailscale: stable
      sagernet/sing-box: [latest, latest-testing]
      finb/bark-server:
      hacdias/webdav:
      dani-garcia/vaultwarden:
      jellyfin/jellyfin:
      # userdocs/iperf3-static:
      # zephyruso/zashboard:
      
